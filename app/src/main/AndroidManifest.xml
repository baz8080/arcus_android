<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          android:installLocation="auto">

    <application
        android:icon="@mipmap/ic_launcher"
        android:name="com.arcuscomputing.dictionary.ArcusApplication"
        android:label="@string/app_name"
        android:theme="@style/ArcusTheme" >

        <activity
            android:name="com.arcuscomputing.dictionary.ArcusSearchActivity"
            android:configChanges="orientation|keyboardHidden"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="stateVisible|adjustPan" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.arcuscomputing.dictionary.FavouritesActivity" />
        <activity android:name="com.arcuscomputing.dictionary.EditPreferencesActivity" />
    </application>

</manifest>